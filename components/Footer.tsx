export default function Footer() {
  return (
    <footer className="mt-16 border-t border-ink-line px-6 py-6 md:px-10">
      <p className="mx-auto max-w-4xl text-center font-mono text-[11px] leading-relaxed text-stone/70">
        Curated demo catalog. Product names and brands are real AllHome partner lines. Images are labeled
        Verified, Representative Example, or Pending Verification depending on what could be confirmed.
        Matching uses standard industry terminology since exact AllHome SKU photography isn&apos;t yet
        available; the same architecture matches directly against real SKU images once connected to
        AllHome&apos;s actual catalog. Pricing isn&apos;t shown because it isn&apos;t published publicly,
        hence the quotation-request flow instead of a price.
      </p>
    </footer>
  );
}
